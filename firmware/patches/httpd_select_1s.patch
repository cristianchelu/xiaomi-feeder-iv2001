--- a/middleware/third_party/httpd/src/axhttpd.c
+++ b/middleware/third_party/httpd/src/axhttpd.c
@@ -146,7 +146,9 @@
 
     while (1)
     {
-        struct timeval tv = { 10, 0 };
+        /* Poll the stop request every second: OTA preflight waits for this
+         * loop to observe HTTPD_STATUS_STOPPING before it frees the task. */
+        struct timeval tv = { 1, 0 };
         FD_ZERO(&rfds);
         FD_ZERO(&wfds);
         FD_ZERO(&efds);

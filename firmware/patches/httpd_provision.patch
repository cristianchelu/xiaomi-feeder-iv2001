diff --git a/middleware/third_party/httpd/src/auth_check.c b/middleware/third_party/httpd/src/auth_check.c
index 4437a243..b2216a09 100644
--- a/middleware/third_party/httpd/src/auth_check.c
+++ b/middleware/third_party/httpd/src/auth_check.c
@@ -82,44 +82,9 @@ void user_config(int lan_updated, int wan_updated)
 
 int auth_check(struct connstruct *cn)
 {
-    char* authpass;
-    int i;
-    char authinfo[MAXREQUESTLENGTH];
-
-    HTTPD_DEBUGF((httpd, "[HTTPD] auth_check() - cn = %p, cn->authorization = %s\n", cn, cn->authorization));
-
-    /* Split into user and password. */
-    memcpy(authinfo, cn->authorization, MAXREQUESTLENGTH);
-    authpass = strchr(authinfo, ':' );
-
-    if ( authpass == (char*) 0 )
-	/* No colon?  Bogus auth info. */
-		goto send_auth;
-    *authpass++ = '\0';
-
-    /* Is this the right user? */
-	for(i=0; i < USER_NUM_MAX; i++)
-	{
-        HTTPD_DEBUGF((
-            httpd, "[HTTPD] auth_check() - (authinfo = %s, authpass = %s), (username = %s, passwd = %s)\n",
-            authinfo, authpass, userinfo[i].username, userinfo[i].passwd));
-        if((strcmp( authinfo, userinfo[i].username) == 0 ) &&
-			( strcmp( authpass, userinfo[i].passwd) == 0 ))
-			break;
-	}
-
-	/* no match username and password */
-	if(i == USER_NUM_MAX)
-	{
-        HTTPD_DEBUGF((httpd, "[HTTPD] auth_check() - no match username and password\n"));
-		goto send_auth;
-	}
-
-	return 0;
-
-send_auth:
-    send_authenticate(cn, cn->server_name);
-	return HTTP_UNAUTHORIZED;
+    /* PetFeeder: httpd runs only during open-AP provisioning — no HTTP auth. */
+    (void)cn;
+    return 0;
 }
 
 static void send_authenticate(struct connstruct *cn, char *realm)
diff --git a/middleware/third_party/httpd/src/axhttpd.c b/middleware/third_party/httpd/src/axhttpd.c
index ff669f6d..20c19d97 100644
--- a/middleware/third_party/httpd/src/axhttpd.c
+++ b/middleware/third_party/httpd/src/axhttpd.c
@@ -125,8 +125,10 @@ int httpd_main(void *argv)
     {
         tp = freeconns;
         freeconns = (struct connstruct *)ax_calloc(sizeof(struct connstruct));
-        if(freeconns == NULL)
+        if (freeconns == NULL) {
             httpd_stop_handle(HTTPD_STOP_REASON_NO_MEMORY);
+            return -1;
+        }
         memset(freeconns, 0, sizeof(struct connstruct));
         freeconns->next = tp;
     }
diff --git a/middleware/third_party/httpd/src/cgi.c b/middleware/third_party/httpd/src/cgi.c
index ee25b07a..2eae13ee 100644
--- a/middleware/third_party/httpd/src/cgi.c
+++ b/middleware/third_party/httpd/src/cgi.c
@@ -39,13 +39,12 @@
 #include <stdio.h>
 #include <string.h>
 
-//MT7687
-#include <hal_rtc.h>
-
 // HTTPD
 #include "os_port.h"
 #include "axhttp.h"
 #include "cgi.h"
+#include "provision_cgi.h"
+#include "web_api_cgi.h"
 
 // FreeRTOS
 #include "hal_sys.h"
@@ -70,13 +69,12 @@
 //==============================================================================
 //                          LOCAL FUNCTION PROTOTYPES
 //==============================================================================
-static int apConfig(struct cgi_para *para);
-static int test2(struct cgi_para *para);
+static int provision_cgi(struct cgi_para *para);
+static int web_api_cgi_entry(struct cgi_para *para);
 
 const struct cgi cgi_cmds[]=
 {
-	{"/apConfig.cgi", &apConfig},
-	{"/Test2.cgi", &test2},
+	{"/provision.cgi", &provision_cgi},
 };
 
 //==============================================================================
@@ -86,6 +84,11 @@ CGI_FUNC cgi_filename_match(char *path_info)
 {
     int i;
 
+    if (path_info != NULL && strncmp(path_info, "/api/", 5) == 0) {
+        web_api_cgi_bind_path(path_info);
+        return web_api_cgi_entry;
+    }
+
     HTTPD_DEBUGF((httpd, "[HTTPD] cgi_filename_match() - path_info = %s\n", path_info));
 
     for (i=0; i< (sizeof(cgi_cmds)/sizeof(struct cgi)) ; i++)
@@ -99,55 +102,13 @@ CGI_FUNC cgi_filename_match(char *path_info)
     return NULL;
 }
 
-static int apConfig(struct cgi_para *para)
+static int provision_cgi(struct cgi_para *para)
 {
-    char buf[MAXREQUESTLENGTH];
-
-    HTTPD_DEBUGF((httpd, "[HTTPD] apConfig() - sd = %d, cmd = %s, cmd_len = %d\n", para->sd, para->cmd, para->cmd_len));
-
-    snprintf(buf, sizeof(buf),
-        "<html><body><h1>apConfig.cgi http response</h1><table><tr><td>cmd = %s</td></tr><tr><td>cmd_len = %d</td></tr></table></body></html>\n",
-        para->cmd, para->cmd_len);
-
-    para->func(para->sd, buf, strlen(buf));
-
-    return 0;
+    return provision_cgi_handler(para);
 }
 
-static int test2(struct cgi_para *para)
+static int web_api_cgi_entry(struct cgi_para *para)
 {
-    char buf[MAXREQUESTLENGTH];
-    struct tm gt;
-    hal_rtc_time_t r_time;
-    char currentTime[32] = "\0";
-    const char * format = "%a, %d %b %Y %H:%M:%S GMT";
-
-    HTTPD_DEBUGF((httpd, "[HTTPD] test2() - sd = %d, cmd = %s, cmd_len = %d\n", para->sd, para->cmd, para->cmd_len));
-
-    memset(&gt, 0, sizeof(struct tm));
-    if(HAL_RTC_STATUS_OK == hal_rtc_get_time(&r_time))
-    {
-        gt.tm_year = r_time.rtc_year; // ?
-        gt.tm_mon = r_time.rtc_mon; // ?
-        gt.tm_mday = r_time.rtc_day;
-        gt.tm_wday = r_time.rtc_week;
-        gt.tm_hour = r_time.rtc_hour;
-        gt.tm_min = r_time.rtc_min;
-        gt.tm_sec = r_time.rtc_sec;
-        strftime(currentTime, sizeof(currentTime), format, &gt);
-    }
-
-    snprintf(buf, sizeof(buf),
-        "<html><head></head><body>"
-        "<h1>Welcome to Test2.cgi</h1>"
-        "<h1>Current system time: %s</h1>"
-        "<form id=\"Test\" action=\"Test.html\" method=\"get\"><input type=\"submit\" value=\"Back\" /></form>"
-        "</body></html>\n",currentTime
-        );
-
-    para->func(para->sd, buf, strlen(buf));
-
-    return 0;
+    return web_api_cgi_handler(para);
 }
 
-
diff --git a/middleware/third_party/httpd/src/proc.c b/middleware/third_party/httpd/src/proc.c
index 1ad4cd80..80905730 100644
--- a/middleware/third_party/httpd/src/proc.c
+++ b/middleware/third_party/httpd/src/proc.c
@@ -52,6 +52,10 @@
    so makefile set file dependence "src/proc.o: webpage.h" */
 #include "webpage.h"
 
+#if WEB_UI_ENABLE
+#include "web_ui.h"
+#endif
+
 static const char * rfc1123_format = "%a, %d %b %Y %H:%M:%S GMT";
 
 static void send_error(struct connstruct *cn, int err);
@@ -394,15 +398,31 @@ void procsendhead(struct connstruct *cn)
 		return ;
 	}
     memset(sendbuf, 0, MAXREQUESTLENGTH);
-    snprintf(sendbuf, MAXREQUESTLENGTH - 1, HTTP_VERSION" 200 OK\r\n"
-        "Server: %s\r\n"
-        "Date: %s\r\n"
-        "Pragma: no-cache\r\n"
-        "Cache-Control: no-cache\r\n"
-        "Content-Type: %s\r\n"
-        "Last-Modified: %s\r\n"
-        "Connection: close\r\n\r\n",
-        date, server_version, getmimetype(cn->actualfile), last_modified);
+#if WEB_UI_ENABLE
+    if (web_ui_is_active()
+        && strcmp(cn->actualfile, CONFIG_HTTP_HOME_HTML) == 0) {
+        snprintf(sendbuf, MAXREQUESTLENGTH - 1, HTTP_VERSION" 200 OK\r\n"
+            "Server: %s\r\n"
+            "Date: %s\r\n"
+            "Pragma: no-cache\r\n"
+            "Cache-Control: no-cache\r\n"
+            "Content-Type: text/html; charset=utf-8\r\n"
+            "Content-Encoding: gzip\r\n"
+            "Connection: close\r\n\r\n",
+            date, server_version);
+    } else
+#endif
+    {
+        snprintf(sendbuf, MAXREQUESTLENGTH - 1, HTTP_VERSION" 200 OK\r\n"
+            "Server: %s\r\n"
+            "Date: %s\r\n"
+            "Pragma: no-cache\r\n"
+            "Cache-Control: no-cache\r\n"
+            "Content-Type: %s\r\n"
+            "Last-Modified: %s\r\n"
+            "Connection: close\r\n\r\n",
+            date, server_version, getmimetype(cn->actualfile), last_modified);
+    }
 
     write_len = special_write(cn->networkdesc, sendbuf, strlen(sendbuf));
     ax_free(sendbuf);
diff --git a/middleware/third_party/httpd/web_c/web__index_html.c b/middleware/third_party/httpd/web_c/web__index_html.c
index 873a651c..ff47cc33 100644
--- a/middleware/third_party/httpd/web_c/web__index_html.c
+++ b/middleware/third_party/httpd/web_c/web__index_html.c
@@ -4,53 +4,20 @@
  */
 
 #include "web_proc.h"
-#include "cgi_api.h"
-
+#include "web_ui.h"
+#include "web_api_cgi.h"
 
 int web__index_html(struct connstruct *cn, char *datap)
 {
+    (void)datap;
 
-	int send_len = 0;
-
-	int total_send_len = 0;
-	send_len = WEB_write_blk(cn, datap, 0, 166);
-
-	if(send_len != 166)
- 		return send_len;
- 	total_send_len += send_len;
-
- CGI_AP_SSID(); 
-
-	send_len = WEB_write_blk(cn, datap, 166, 51);
-
-	if(send_len != 51)
- 		return send_len;
- 	total_send_len += send_len;
-
- CGI_AP_PASSWD(); 
-
-	send_len = WEB_write_blk(cn, datap, 217, 48);
-
-	if(send_len != 48)
- 		return send_len;
- 	total_send_len += send_len;
-
- CGI_AP_SSID(); 
-
-	send_len = WEB_write_blk(cn, datap, 265, 52);
-
-	if(send_len != 52)
- 		return send_len;
- 	total_send_len += send_len;
-
- CGI_AP_PASSWD(); 
-
-	send_len = WEB_write_blk(cn, datap, 317, 1515);
-
-	if(send_len != 1515)
- 		return send_len;
- 	total_send_len += send_len;
-
-	return total_send_len;
+    if (web_ui_is_active()) {
+        return web_ui_index_serve(cn);
+    }
 
+    return WEB_printf(cn,
+                      "<!DOCTYPE html><html><head>"
+                      "<meta http-equiv=\"refresh\" content=\"0;url=/provision.cgi\">"
+                      "<title>PetFeeder Setup</title></head>"
+                      "<body><p><a href=\"/provision.cgi\">PetFeeder Setup</a></p></body></html>");
 }

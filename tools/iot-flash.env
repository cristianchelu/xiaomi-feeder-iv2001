# Sourced automatically by tools/iot-flash.sh (does not override env already set).

export IOT_FLASH_RESET_WAIT_SEC="${IOT_FLASH_RESET_WAIT_SEC:-30}"
export IOT_FLASH_LISTEN_MS="${IOT_FLASH_LISTEN_MS:-2000}"
